# Correction-2 prompt: owner, generative correspondence, admission, reservation, and release

You are reviewing the W1-11/W1-12 libbun retained-provider full SCC. Exact libbun product source is 6066a5b85a0c6d1f6397914b8666b0fd0e5fd7eb, tree cb964de8ab8162449fbe95959bf34d231570aa5c. Correction-2 review evidence is based on bundle commit ec6a7f249120a833aeaa4e0211fe0f41d17e0565. Exact adjacent Swarm source is 95323ff17cb29928e31467f651ef03bae2099c14, tree 43b47bbd49a6053d270b3e15cc141cb1b1bb86da. The poison cut is complete: install_prepared_export is deleted and external compile-fail tripwires exist, while no positive retained-backend implementation exists. Treat the frozen contracts, three independent REVISE verdict records, deterministic search reports, exact source snapshots, and current source as authoritative. The deliverable is CONCRETE IMPLEMENTATION: provide an exact-source-applicable unified diff or commit-grade code across every repository that must move, the exact first positive edit after poison, typed-fault and one-shot custody behavior, migration/deletion order, hostile default-parallel tests, and exact compile/search/package commands. REVISE or REJECT must still provide the clean replacement and largest valid patch plus the exact missing file/symbol/fact list.

The prior owner review is BUNDLE REVISE at bd86b8863ed21c19fa46bfdf1a006d8a83ff0330. Its determining omission is now attached: the real exact-route producer ManifestResolvedExternalProviderCallAuthority, the DurableExternalProviderInvocationAuthority input/output-settlement producer, SsExternalCapabilityProviderHost as sole libbun consumer, the external transport trait/session, and their Cargo dependency graph. Current source publicly splits the sealed invocation, extracts route/contract material, constructs JavaScript adapter source and ProviderRequest, and then calls a dynamic libbun API. A libbun-private constructor cannot be called by that producer; a public raw constructor, parts product, selector, callback proof, or caller-chosen receipt is forbidden. Treat a libbun-only generative reconstruction as impossible.

Preserve opaque by-value BunProviderBackend as the highest runtime owner. Select one acyclic concrete owner boundary that can consume the exact manifest-resolved call, provider input, output-settlement authority, and retained backend without public RAW. Move ownership/dependencies and the generative mint there; if no lawful placement exists, explicitly mark the current decomposition invalid and give the owner-boundary replacement rather than adding a bridge. Generatively co-brand move-only SelectedProviderPackage<Brand> and ProviderInvocation<Brand>; differently branded inputs must not compose. Private OfferCustody and OfferReadyProof govern refusal and unchanged retry on the same Ready worker/epoch. Private ReservedCustody owns one dispatch permit; affine PreparedExport can drive, proof-bound release through ReservationReleaseProof, or consuming shutdown. Ambiguous release retires or transfers by value to the durable queue.

The exact first positive libbun edit after poison is src/lib.rs: introduce opaque BunProviderBackend with private Ready or Restartable custody and establish the process-wide preallocated durable queue before a live backend can be constructed. Then provide the exact adjacent Swarm owner/dependency move and delete into_contract_and_module_for_durable_external_provider_owner_v1, into_call_input_and_output_settlement_for_durable_external_provider_owner_v1 if its split is no longer lawful, adapter-source/request reconstruction, dynamic-loading compatibility, and stale callers in the same migration. Return hostile privacy, mismatched-brand, replay, exact correspondence, refusal/retry, pre-dispatch release, cancellation/unwind, and consuming-shutdown tests. No public raw constructor, Clone, serde, parts, path/export getter, borrowed AUTH mint, trait callback proof, or backend husk is allowed.
