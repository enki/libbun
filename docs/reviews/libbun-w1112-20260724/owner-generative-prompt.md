# Correction-3 prompt: owner, W1-10 input, generative correspondence, admission, reservation, and release

You are reviewing the W1-11/W1-12 libbun retained-provider full SCC. Exact libbun product source is 6066a5b85a0c6d1f6397914b8666b0fd0e5fd7eb, tree cb964de8ab8162449fbe95959bf34d231570aa5c. Correction-3 review evidence descends from correction-2 commit 51b0118428d7881f39f32df396ef32349a5a52ab. Exact adjacent Swarm source is 95323ff17cb29928e31467f651ef03bae2099c14, tree 43b47bbd49a6053d270b3e15cc141cb1b1bb86da. The poison cut is complete: install_prepared_export is deleted and external compile-fail tripwires exist, while no positive retained-backend implementation exists. Treat the frozen contracts, three correction-2 independent REVISE verdicts, Lane-independent deterministic evidence, exact source snapshots, and current source as authoritative. The deliverable is CONCRETE IMPLEMENTATION: provide an exact-source-applicable unified diff or commit-grade code across every repository that must move, the exact first positive edit after poison, typed-fault and one-shot custody behavior, migration/deletion order, hostile default-parallel tests, and exact compile/search/package commands. REVISE or REJECT must still supply the clean replacement and largest valid patch plus the exact missing file/symbol/fact list.

The correction-2 owner review is BUNDLE REVISE at 47acbee0fe5a67231969efd8141ba8195bcecc8a. Its complete correction is now attached: W1-10 ProviderValue source/law/Cargo/negative fixture; all thirteen prior adjacent snapshots; the exact-route and invocation producers; SsExternalCapabilityProviderHost as sole libbun consumer; the external transport/session; the ss-test retained-host checkout/replacement/shutdown chain; and every direct Cargo boundary. Current source publicly splits the sealed invocation, extracts route/contract material, constructs JavaScript adapter source and ProviderRequest, and calls a dynamic libbun API. A libbun-private constructor cannot be called by that producer; a public raw constructor, parts product, selector, callback proof, or caller-chosen receipt is forbidden. Treat a libbun-only generative reconstruction as impossible.

Preserve opaque by-value BunProviderBackend as the highest runtime owner. W1-10 ProviderValue is the sole by-value invocation input cargo. ProviderInvocation<Brand> seals that ProviderValue with the exact selected call and output-settlement authority. No JSON/Value/string/bytes/StructuralValue parallel input, public conversion, re-admission, parts getter, or callback may replace it. Select one acyclic concrete owner boundary that consumes the exact manifest-resolved call, ProviderValue, output-settlement authority, and retained backend without public RAW. Move ownership/dependencies and the generative mint there; if no lawful placement exists, mark the current decomposition invalid and provide the owner-boundary replacement. Generatively co-brand move-only SelectedProviderPackage<Brand> and ProviderInvocation<Brand>; differently branded inputs must not compose.

PreparedExport::drive is the one mechanical drive owner. The retained backend moves that existing drive under private ReservedCustody/DriveCustody; no second drive, compatibility drive, callback driver, or caller-minted receipt is introduced. The settlement boundary remains mechanical-only and language-authored settlement stays with the adjacent Swarm owner. Private OfferCustody and OfferReadyProof govern refusal and unchanged retry on the same Ready worker/epoch. Private ReservedCustody owns one dispatch permit; affine PreparedExport can drive, proof-bound release through ReservationReleaseProof, or consuming shutdown. Ambiguous release retires or transfers by value to the durable queue.

Every typed refusal/fault/cancellation/unwind/shutdown terminal owns the exact backend, branded invocation cargo, reservation/dispatch permit, process, containment, pipe, pump, channel, join, output ledger, and settlement custody required by its phase. No terminal returns RAW parts or a backend husk. The exact first positive libbun edit after poison is src/lib.rs: introduce opaque BunProviderBackend with private Ready or Restartable custody and establish the process-wide preallocated durable queue before a live backend can be constructed. Then provide the exact adjacent Swarm owner/dependency move and delete the raw splitters, adapter/request reconstruction, dynamic-loading compatibility, and stale callers in the same migration. Return hostile privacy, mismatched-brand, replay, exact correspondence, refusal/retry, pre-dispatch release, cancellation/unwind, Drop, shutdown, and retained-host replacement tests.
