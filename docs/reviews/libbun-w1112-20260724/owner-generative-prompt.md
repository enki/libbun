# Correction-6 prompt: owner, W1-10 input, generative correspondence, mechanical drive, final output, and release

You are reviewing the W1-11/W1-12 libbun retained-provider full SCC. Exact libbun product source is `6066a5b85a0c6d1f6397914b8666b0fd0e5fd7eb`, tree `cb964de8ab8162449fbe95959bf34d231570aa5c`. Correction 6 repairs only the owner/correspondence source-closure omission found in exact correction-5 candidate and independent-verdict SHA `5e74c14a0125c1670be7e37cc31675ebedcd538d`, tree `72f5a93fc3c207ccac42a1113959eb6e1d33e9b3`. Exact adjacent Swarm source is `95323ff17cb29928e31467f651ef03bae2099c14`, tree `43b47bbd49a6053d270b3e15cc141cb1b1bb86da`. The correction-5 owner verdict is literal `PART BUNDLE REVISE`; its lifecycle and containment/release verdicts are literal `PART BUNDLE PASS` and remain controlling. The poison cut is complete: `install_prepared_export` is deleted and external compile-fail tripwires exist, while no positive retained-backend implementation exists.

The deliverable is CONCRETE IMPLEMENTATION: return an exact-source-applicable unified diff or commit-grade code across every repository that must move, the exact first positive edit after poison, typed-fault and one-shot custody behavior, migration/deletion order, hostile default-parallel tests, and exact compile/search/package commands. REVISE or REJECT must still supply the clean replacement and largest valid patch plus the exact missing file/symbol/fact list.

The attached discovery preserves the 24-path lexical regression set and independently binds the exact ordered 27-path semantic owner search plus all 16 `ProviderHostExecutionSession` producer/carrier/consumer paths. It closes both source-entrypoint `begin_provider_execution_session_v1` mints, by-value prepared-runtime admission/refusal and admitted terminal drive, every borrowed process-child/start/reawaken/provider-resume carrier, `host_resource_finalization.rs` exact release correspondence, and the final publication consumer. The complete selection seal, both Drop implementations, reissue and consume operations, exact provider-release commit, twenty-thousand-drop reissue, unwind reissue, provider refusal/retry, foreign cross-splice refusal, and `process_session_provider_commit_unwind_retains_exact_custody_through_retry_publication` are source-bound. The source also binds the existing `mint_provider_boundary_output_correspondence_v1` join and its typed `ProviderBoundaryIngressFault::OutputCorrespondence` terminal. Mechanical quiescence consumers and adjacent Cargo/privacy edges are also bound. Capability-model source contains mismatch and custody-preservation tests; static-host source contains nominal accepted/rejected/authored correspondence tests. It contains no static-host mismatch or replay test, so new static-host mismatch and retained-libbun replay tests remain mandatory. The new generatively branded `SelectedProviderPackage<Brand>` / `ProviderInvocation<Brand>` law must extend this exact occurrence brand or atomically replace the existing mint and join. A parallel package/invocation brand beside the pending/selected output seal is forbidden.

Preserve opaque by-value `BunProviderBackend` as the highest runtime product. `W1-10 ProviderValue is the sole by-value invocation input cargo`. `ProviderInvocation<Brand>` seals it with the exact selected call and output-settlement authority. No JSON/Value/string/bytes parallel input, public conversion, re-admission, parts getter, callback proof, public selector, or caller-minted receipt may replace it. The finite semantic owner is `swarm_provider_host_set::ProviderHostExecutionSession`: the attachment includes its fields, constructor, public reexport, durable-loader mint/caller and Cargo direction, selected external branch, and `Drop` shutdown. It already owns prepared-runtime exact-route admission, receives the sealed ProviderValue plus selected-output half, chooses the external domain, and retains the external execution session across calls. This is the acyclic concrete owner boundary; if it cannot own the operation, move the whole external-domain session owner instead of adding a bridge.

The owner operation consumes `ManifestResolvedExternalProviderCallAdmission + ProviderValue + SelectedProviderBoundaryOutputAuthority + BunProviderBackend`, preserves one occurrence brand across exact package/call/invocation/output correspondence, runs `PreparedExport::drive` as the sole one-shot mechanical drive under private `OfferCustody`, `OfferReadyProof`, `ReservedCustody`, `ReservationReleaseProof`, and `DriveCustody`, and returns only `SelectedProviderBoundaryExecutionResultForProviderHostOwner` or a typed custody-retaining mechanical refusal/fault/retirement/shutdown terminal. Settlement remains mechanical-only; language-authored settlement stays with the adjacent Swarm owner. Treat libbun-only generative reconstruction as impossible. Do not add a raw selector/parts bridge, callback proof, borrowed authority mint, fallback, placeholder fault, or semantic settlement in libbun.

Delete the callback proof, public raw splitters, route match aperture, adapter-source/ProviderRequest reconstruction, dynamic-loading/plugin compatibility callers, caller-selected receipts, and every backend-husk terminal in the same migration. Hostile tests must cover privacy, mismatched brands and output halves, replay, refusal/unchanged retry, pre-dispatch release, process/VM interruption, cancellation/unwind, Drop, process exit, shutdown, exact final-output correspondence, outer Swarm mechanical success/fault settlement, and retained-host replacement.
