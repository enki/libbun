# Oracle/Fable prompt: owner, generative correspondence, admission, reservation, and release

You are reviewing libbun at exact candidate 6066a5b85a0c6d1f6397914b8666b0fd0e5fd7eb, tree cb964de8ab8162449fbe95959bf34d231570aa5c. The candidate is the completed poison cut after the rejected W1-11/W1-12 composition: the raw installer is deleted and external compile-fail tripwires exist, but no positive retained-backend implementation exists. Treat the frozen contracts, poison evidence, exact verdict snapshot, source, and search reports as authoritative. Do not invent missing constructors or assume a later tree. The deliverable is CONCRETE IMPLEMENTATION: return an exact-SHA-applicable unified diff or commit-grade replacement code, the exact first positive source edit after poison, migration/deletion order, typed faults and one-shot custody behavior, hostile tests, compile/search commands, and a source-grounded reason for every genuinely impossible hunk. A REVISE or REJECT answer must still supply the clean replacement and largest valid patch plus the exact missing file/symbol/fact list. Preserve Rust ownership of runtime authority and keep observations from feeding authority.

Review only the owner/generative tranche while keeping its lifecycle interfaces compatible with the frozen complete algebra. Produce the opaque by-value BunProviderBackend and generatively co-branded SelectedProviderPackage<Brand> plus ProviderInvocation<Brand>. Design private OfferCustody and OfferReadyProof, private ReservedCustody with an unconsumed dispatch permit, affine PreparedExport, sealed ReservationReleaseProof, closed refusal/admission/retry/release terminals, and consuming shutdown entry. The same selected offer must be retryable unchanged after proof-bound refusal; ambiguous release must retire or transfer custody to the durable queue. No public raw constructors, selectors, Clone, serde, parts, callback proof, borrowed AUTH mint, worker path/export projection, or caller-minted receipt is allowed.

Name every producer, constructor, carrier, sole consumer, and existing or required typed fault. Show generative correspondence in code so differently branded package/invocation values cannot compose. The exact first positive edit is the first source edit after the already-completed installer poison cut; do not repeat that deletion. Return commit-grade changes and default-parallel focused tests including external privacy tripwires, refusal with unchanged retry, pre-dispatch release, mismatched-brand compile failure, cancellation/unwind custody, and consuming shutdown boundaries. State how this tranche hands private custody to lifecycle/quarantine without exposing RAW.
