# Correction-2 prompt: private native/wire containment, persistent output, retained worker, locks, packaging, and release

You are reviewing the W1-11/W1-12 libbun retained-provider full SCC. Exact libbun product source is 6066a5b85a0c6d1f6397914b8666b0fd0e5fd7eb, tree cb964de8ab8162449fbe95959bf34d231570aa5c. Correction-2 review evidence is based on bundle commit ec6a7f249120a833aeaa4e0211fe0f41d17e0565. Exact adjacent Swarm source is 95323ff17cb29928e31467f651ef03bae2099c14, tree 43b47bbd49a6053d270b3e15cc141cb1b1bb86da. The poison cut is complete: install_prepared_export is deleted and external compile-fail tripwires exist, while no positive retained-backend implementation exists. Treat the frozen contracts, three independent REVISE verdict records, deterministic search reports, exact source snapshots, and current source as authoritative. The deliverable is CONCRETE IMPLEMENTATION: provide an exact-source-applicable unified diff or commit-grade code across every repository that must move, the exact first positive edit after poison, typed-fault and one-shot custody behavior, migration/deletion order, hostile default-parallel tests, and exact compile/search/package commands. REVISE or REJECT must still provide the clean replacement and largest valid patch plus the exact missing file/symbol/fact list.

The prior containment/release review is BUNDLE REVISE at 54241a683e1c68366715456b4517bcf2966bbdf7. Correction 2 fixes every determining bundle omission: the vendored identity generator now records SHA-256, Git blob, bytes, path, and excerpt spans without awk; every exact search records its literal command, pattern, pathspec, meaning, expected exit, observed exit, and output; all four nonvendored locks and the complete six-file privacy harness are direct attachments; LICENSE, vendor provenance, Bun license, and vendored Cargo.lock are direct compliance inputs.

Implement under opaque by-value BunProviderBackend. Move wire framing into private facade/runtime modules and delete the public wire crate/API. Move the native engine into binary-only runtime ownership and delete the native library, internal-adapter feature, and public Rust drive entry point. Establish exact pre-dispatch Linux namespace containment, macOS spawn-denying sandboxing, and atomic Windows job assignment; refuse unsupported platforms and never fall back to process groups. Install persistent bounded stdout/stderr/Bun-output/log pumps before descriptors reach Bun, keep draining after bounded overflow, use per-invocation barriers, and reserve EOF/join for retirement.

Implement retained same-worker/same-epoch offer/reserve/release/dispatch/cooperative-cancel/ready reuse, second invocation, replacement epoch after proof-bound retirement, fault dominance, undefined/null distinction, authored rejection settlement, and bounded finalization. Make linked execution mandatory and remove warning-only unlinked builds. Produce one coherent lock topology, packageable facade and binary, privacy/symbol gates, deterministic compliance inventory, immutable-tag worker-only release workflow, immutable archive inputs, and complete freshly extracted smoke execution. Delete fresh-process-only and fallback fields.

Return exact commit-grade source/workflow changes in frozen hard-cut order plus direct, real-worker, containment, output saturation/barrier, reaper integration, privacy, vendored reproducibility, linked nonzero engine, repeat-lock/package, compliance, archive, extracted-smoke, immutable-tag release, and stale-symbol tests. No public wire/native handle, raw descriptor, callback proof, process-group fallback, blocking Drop, or caller-minted receipt may survive.
