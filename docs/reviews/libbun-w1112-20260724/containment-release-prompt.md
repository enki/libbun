# Correction-5 prompt: atomic deletion/tests, containment, retained-runtime final close, persistent output, packaging, and release

Review W1-11/W1-12 at exact libbun product source `6066a5b85a0c6d1f6397914b8666b0fd0e5fd7eb`, tree `cb964de8ab8162449fbe95959bf34d231570aa5c`, correction-4 candidate `4dd3395129a221d8c1fb2d1dbbdae509b2331f0e`, and exact adjacent Swarm source `95323ff17cb29928e31467f651ef03bae2099c14`, tree `43b47bbd49a6053d270b3e15cc141cb1b1bb86da`. The correction-4 containment verdict is literal `PART BUNDLE REVISE` at `d6f9ae079eea0d635115fabae13526b29266b491`. Return CONCRETE IMPLEMENTATION with an exact-source-applicable unified diff or commit-grade code, exact first edit, typed custody, migration/deletion order, hostile default-parallel tests, packaging/release commands, and a clean replacement even for REVISE or REJECT.

`BunProviderBackend` owns the process, pipes, persistent bounded pumps, overflow accounting, EOF/barrier protocol, joins, settlement ledger, and exact platform containment from construction through consuming shutdown. Linux namespace/cgroup containment, macOS process containment, and Windows job assignment must be established before executable work. Process-group fallback or a caller-minted receipt is forbidden. Same-worker reuse requires proof-bound drain; replacement epoch requires proof-bound retirement.

The attached retained-host sources expose `ExternalCapabilityProviderPool -> ProviderSettlementLane -> SsRuntimeExecutionDomainOwner::shutdown`, the live-feed close, both outer `close_for_execution_graph_owner` carriers, their module/privacy boundary, and `produce_graph_close_receipt_for_execution_graph_owner`, the sole final success/fault consumer. That consumer first reaps the runtime-file pool, consumes graph settlements and the live feed, then either consumes the runtime-file session into final success or restores it while marking closeout failed. Repair the stale edge where the inner close consumes the runtime-domain owner without calling the separately defined `shutdown_runtime_execution_domain_owner`. Preserve checkout/replacement custody and make final close consume the retained runtime exactly once. A shutdown failure after live-feed consumption must return a typed custody terminal; it cannot retry from a consumed backend, reconstruct authority, or lose process/output containment on unwind/Drop. The full real-worker Rust fixture is authoritative for `ss_reuses_one_libbun_runtime_for_multiple_capability_imports`, the pool-child package-root case, and atomic deletion of stale `current_native_plugin_asset()` / `plugin/target/release` migration.

The release design is governed by `docs/LIBBUN-WORKER-RELEASE-CONTRACT.md` and the complete active implementations `scripts/package-prepared-export-worker-release.sh`, `scripts/prepare-native-bun-link.sh`, `scripts/verify-vendored-bun-reproducible.sh`, and `scripts/verify-vendored-bun.sh`. Return exact changes for immutable-tag archive construction, deterministic manifest/hashes/order/cleanup, linked-input validation, freshly extracted smoke execution, repeat-lock behavior, provenance/compliance, and the worker-only release workflow. Preserve all four nonvendored locks, the complete six-file privacy harness, vendored workspace/lock, and the exact-tree Cargo/license inventory.

Hostile tests cover refusal/retry, cancellation/deadline/unwind, overflow, missing EOF/barrier, pump/join failure, containment-assignment failure, process escape, replacement races, final retained-host shutdown, stale-plugin removal, package reproducibility, immutable-tag refusal, and freshly extracted linked-worker smoke. Every terminal consumes or durably adopts all process/output/containment custody without raw handles, parts, selectors, or backend husks.
