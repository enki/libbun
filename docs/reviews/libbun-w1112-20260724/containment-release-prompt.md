# Oracle/Fable prompt: private native/wire containment, output pumps, retained worker, packaging, and hostile tests

You are reviewing libbun at exact candidate 6066a5b85a0c6d1f6397914b8666b0fd0e5fd7eb, tree cb964de8ab8162449fbe95959bf34d231570aa5c. The candidate is the completed poison cut after the rejected W1-11/W1-12 composition: the raw installer is deleted and external compile-fail tripwires exist, but no positive retained-backend implementation exists. Treat the frozen contracts, poison evidence, exact verdict snapshot, source, and search reports as authoritative. Do not invent missing constructors or assume a later tree. The deliverable is CONCRETE IMPLEMENTATION: return an exact-SHA-applicable unified diff or commit-grade replacement code, the exact first positive source edit after poison, migration/deletion order, typed faults and one-shot custody behavior, hostile tests, compile/search commands, and a source-grounded reason for every genuinely impossible hunk. A REVISE or REJECT answer must still supply the clean replacement and largest valid patch plus the exact missing file/symbol/fact list. Preserve Rust ownership of runtime authority and keep observations from feeding authority.

Review and implement the private execution and release tranche under the retained BunProviderBackend algebra. Move wire framing into private modules compiled only by the facade owner and binary, delete the public wire crate/API, move the native engine into the binary-only runtime, and delete the native library, internal-adapter feature, and public Rust drive entry point. Implement exact pre-dispatch platform containment with Linux namespace containment, macOS spawn-denying sandboxing, and atomic Windows job assignment; unsupported platforms refuse before dispatch and there is no process-group fallback. Install persistent bounded stdout/stderr pumps before descriptors reach Bun, keep draining on overflow, provide per-invocation barriers, and reserve EOF/join for retirement.

Implement retained same-worker/same-epoch offer/reserve/release/dispatch/cancel/ready reuse, second invocation, replacement epoch after proof-bound retirement, fault dominance, and bounded finalization. Correct JS rejection settlement and preserve undefined versus null. Make linked execution mandatory; remove warning-only unlinked builds. Produce packageable facade and binary, one coherent lock strategy including wire deletion, binary privacy/symbol gates, compliance inventory, immutable-tag worker-only release workflow, archive from immutable inputs, and a complete freshly extracted package smoke protocol. Remove fresh-process-only and fallback fields. Include direct, real-worker, containment, output saturation/barrier, reaper integration, privacy, vendored Bun reproducibility, linked nonzero engine, repeated lock/package, archive, extracted smoke, and release tests. Return exact-SHA-applicable commit-grade code, workflow and migration/deletion order, using the narrow vendored-boundary report instead of guessing Bun internals.
