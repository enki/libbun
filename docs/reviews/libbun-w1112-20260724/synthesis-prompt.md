# Correction-5 prompt: W1-11/W1-12 full-SCC synthesis (BLOCKED)

Synthesis is BLOCKED: do not launch a synthesis model until fresh literal `PART BUNDLE PASS` verdicts exist for all three correction-5 parts. Once unblocked, synthesize one CONCRETE IMPLEMENTATION against exact libbun product source `6066a5b85a0c6d1f6397914b8666b0fd0e5fd7eb`, tree `cb964de8ab8162449fbe95959bf34d231570aa5c`, correction-4 candidate `4dd3395129a221d8c1fb2d1dbbdae509b2331f0e`, and exact adjacent Swarm source `95323ff17cb29928e31467f651ef03bae2099c14`, tree `43b47bbd49a6053d270b3e15cc141cb1b1bb86da`. All three correction-4 independent verdicts are literal `PART BUNDLE REVISE`; correction 5 attaches their source-grounded replacement. Return an exact-source-applicable unified diff or commit-grade code, exact first edit, typed faults and custody, migration/deletion order, hostile default-parallel tests, compile/search/package commands, and the largest valid patch plus exact missing facts for any impossible hunk.

The complete transition is producer -> exact selected call -> `W1-10 ProviderValue` -> one occurrence brand spanning existing output correspondence and generatively branded package/invocation -> opaque `BunProviderBackend` -> private offer/reservation/drive custody -> sole `PreparedExport::drive` -> typed drive-to-final-output carriers -> `ProviderBoundaryIngressFault::OutputCorrespondence` -> corresponded final ready output -> outer Swarm mechanical-only settlement and final success/fault observation. The finite external-domain owner is `ProviderHostExecutionSession`, including its fields, constructor, reexport, loader caller, selected branch, and Drop; a parallel brand, public RAW, parts, callback proof, selector, dynamic compatibility path, or caller-minted receipt is forbidden.

Reuse requires complete quiescence across invocation, microtasks, diagnostics, persistent output, finalization, and child/nested workers. Both BunProcess process-exit callers and `NodeVMModule::evaluate` timeout/SIGINT reset are bound. The concrete process path is process exit -> `global_exit` or WebWorker exit -> terminate-all-and-wait -> live-worker unregister -> ordered shutdown. Termination reset and `JSC__VM__deinit` are not proof. Every ambiguous cancellation, timeout, exception unwind, Drop, output/join failure, or active worker transfers intact retirement/quarantine/durable reaper custody; shutdown-origin custody never recovers.

Containment is exact per platform; output pumps are persistent bounded state; final retained-host close consumes the runtime once through both outer carriers and the final success/fault consumer. Failure after feed consumption must not retry a consumed backend. Release uses immutable-tag deterministic packaging, active link/reproducibility scripts, all lock/privacy/license inputs, and freshly extracted smoke. Apply the frozen Fifteen-Step Hard-Cut: create the durable sink before live backend construction, move the finite owner/dependencies, migrate all callers/tests, atomically delete callback/raw/plugin compatibility, prove privacy and old-symbol absence, then package and smoke the linked worker.
