# Correction-4 prompt: W1-11/W1-12 full-SCC synthesis

Synthesize one CONCRETE IMPLEMENTATION against exact libbun product source `6066a5b85a0c6d1f6397914b8666b0fd0e5fd7eb`, tree `cb964de8ab8162449fbe95959bf34d231570aa5c`, correction-3 candidate `c2ea016e4c9810fa86ddfd21bd4b30823746a9b9`, and exact adjacent Swarm source `95323ff17cb29928e31467f651ef03bae2099c14`, tree `43b47bbd49a6053d270b3e15cc141cb1b1bb86da`. All three correction-3 independent verdicts are literal `PART BUNDLE REVISE`; correction 4 attaches their clean source-complete replacement. Return an exact-source-applicable unified diff or commit-grade code, exact first edit, typed faults and custody, migration/deletion order, hostile default-parallel tests, compile/search/package commands, and the largest valid patch plus exact missing facts for any impossible hunk.

The complete transition is producer -> exact selected call -> `W1-10 ProviderValue` -> one occurrence brand spanning existing output correspondence and generatively branded package/invocation -> opaque `BunProviderBackend` -> private offer/reservation/drive custody -> sole `PreparedExport::drive` -> mechanical-only settlement -> corresponded final ready output. The finite external-domain owner is `ProviderHostExecutionSession`; a parallel brand, public RAW, parts, callback proof, selector, dynamic compatibility path, or caller-minted receipt is forbidden.

Reuse requires complete quiescence across invocation, microtasks, diagnostics, persistent output, finalization, and child/nested workers. The concrete process path is process exit -> `global_exit` or WebWorker exit -> terminate-all-and-wait -> live-worker unregister -> ordered shutdown. `JSC__VM__deinit` is not proof. Every ambiguous cancellation, timeout, unwind, Drop, output/join failure, or active worker transfers intact retirement/quarantine/durable reaper custody; shutdown-origin custody never recovers.

Containment is exact per platform; output pumps are persistent bounded state; final retained-host close consumes the runtime once. Release uses immutable-tag deterministic packaging, active link/reproducibility scripts, all lock/privacy/license inputs, and freshly extracted smoke. Apply the frozen Fifteen-Step Hard-Cut: create the durable sink before live backend construction, move the finite owner/dependencies, migrate all callers/tests, delete callback/raw/plugin compatibility, prove privacy and old-symbol absence, then package and smoke the linked worker.
