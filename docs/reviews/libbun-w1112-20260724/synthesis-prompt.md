# Oracle/Fable prompt: W1-11/W1-12 full-SCC synthesis

You are reviewing libbun at exact candidate 6066a5b85a0c6d1f6397914b8666b0fd0e5fd7eb, tree cb964de8ab8162449fbe95959bf34d231570aa5c. The candidate is the completed poison cut after the rejected W1-11/W1-12 composition: the raw installer is deleted and external compile-fail tripwires exist, but no positive retained-backend implementation exists. Treat the frozen contracts, poison evidence, exact verdict snapshot, source, and search reports as authoritative. Do not invent missing constructors or assume a later tree. The deliverable is CONCRETE IMPLEMENTATION: return an exact-SHA-applicable unified diff or commit-grade replacement code, the exact first positive source edit after poison, migration/deletion order, typed faults and one-shot custody behavior, hostile tests, compile/search commands, and a source-grounded reason for every genuinely impossible hunk. A REVISE or REJECT answer must still supply the clean replacement and largest valid patch plus the exact missing file/symbol/fact list. Preserve Rust ownership of runtime authority and keep observations from feeding authority.

Synthesize the three independently source-grounded part reviews under the opaque by-value BunProviderBackend into one closed implementation for the complete producer -> branded admission -> offer -> reservation -> dispatch -> retained native worker -> authored/mechanical settlement -> retirement/quarantine/reaper -> restart or consuming shutdown SCC. Reconcile type names, generative brands, fault algebra, custody transitions, crate privacy, platform containment, output pumps, packaging, locks, workflows, and hostile tests. Reject any part proposal that exposes RAW, duplicates minting, permits borrowed AUTH, loses custody on refusal/retry/cancel/unwind/Drop/shutdown, publishes a fault before durable adoption, blocks in Drop, uses process-group fallback, retains public wire/native APIs, or packages a fresh-process fallback.

Deliver CONCRETE IMPLEMENTATION as one exact-SHA-applicable unified diff or commit-grade code in the frozen Fifteen-Step Hard-Cut order. Identify the exact first positive edit after poison, all constructors and sole consumers, complete fallible staging and infallible commits, typed fault dominance, deletion order, hostile default-parallel tests, and exact compile/search/package/release commands. A non-PASS verdict must still return the clean replacement and largest valid patch with the exact missing file/symbol/fact list.
