PART BUNDLE REVISE

# W1-11/W1-12 Correction-2 Lifecycle Bundle Review

Date: 2026-07-24

## Bound review identity

- Review bundle commit: `51b0118428d7881f39f32df396ef32349a5a52ab`
- Review bundle tree: `55f063f9df9491aa6893c233eb2f15950bb428b2`
- Exact product source: `6066a5b85a0c6d1f6397914b8666b0fd0e5fd7eb`
- Exact product tree: `cb964de8ab8162449fbe95959bf34d231570aa5c`
- Adjacent swarm source: `95323ff17cb29928e31467f651ef03bae2099c14`
- Adjacent swarm tree: `43b47bbd49a6053d270b3e15cc141cb1b1bb86da`
- Lifecycle manifest SHA-256: `dc66093ef4968aab585d56bdec50d460cb48dcb712b927ac7da664a6e5c94adc`
- Ordered file-plan SHA-256: `35a1d3e4a35abb0f572df02880189808cfa7fa8b8dbef25ff5cc644567d224ea`
- Prompt SHA-256: `6f2ffb4a5fce4bd7871aba553aab5ade1389be8b97994cd558afa0958c176855`
- Oracle dry-run SHA-256: `d5ed88eb68d70d0058f44f93e70352e239e5f4e519345a4711c8672ecae37386`
- Fable plan SHA-256: `cd87ae3569e8b55f2507287a1a01e5feaf10e72c41803d2915db81266682cf5d`
- Evidence generator SHA-256: `0069166626e368d5904b7b7810a21bf0b64f99e996933f07452b36180fadf6af`
- Bundle verifier SHA-256: `f28861258bacc40e45b36b940ac880a0ffe58c053ed2bccf3ed65bc55f8487b6`
- Model launch: none; Oracle and Fable remain `NOT LAUNCHED`.

The product/test/Cargo/workflow delta from the exact product source is zero.
The adjacent producer, invocation authority, sole libbun consumer, provider
host session `Drop`, shutdown boundary, Rust fixture, and two `.ss` fixtures
are attached at the bound swarm SHA. Those additions close the prior caller
and fixture omission. The correction-2 lifecycle bundle still cannot receive
`PART BUNDLE PASS` because its required deterministic replay fails in the
independent Lane and its ordered attachments do not match its full-source
claim.

## Determining defects

### 1. Deterministic evidence replay embeds the generating Lane

`scripts/generate-libbun-w1112-review-evidence-20260724.py::command_text`
serializes `ROOT` into every libbun `git -C` command. The two checked-in
reports therefore contain the bundle-authoring Lane:

```text
/home/ubuntu/bridge-ops/dev-worktrees/libbun-w1112-bundle-correction2-20260724
```

The same generator run from this independently claimed review Lane emits:

```text
/home/ubuntu/bridge-ops/dev-worktrees/libbun-w1112-c2-rv-lifecycle-20260724
```

The searched SHA, pattern, pathspec, exit, and search output are otherwise the
same, but the generated bytes are Lane-dependent. Consequently the advertised
fresh-Lane verification gate fails before it can validate the bundle:

```text
$ python3 scripts/generate-libbun-w1112-review-evidence-20260724.py --check
DRIFT docs/reviews/libbun-w1112-20260724/exact-source-search-report.md
DRIFT docs/reviews/libbun-w1112-20260724/process-drop-caller-and-fixture-report.md
OK docs/reviews/libbun-w1112-20260724/vendored-bun-boundary-report.md
OK docs/reviews/libbun-w1112-20260724/adjacent-swarm-source-index.md
OK docs/reviews/libbun-w1112-20260724/lifecycle-vendored-jsc-source-bundle.md
OK docs/reviews/libbun-w1112-20260724/lock-privacy-compliance-index.md
exit 1
```

```text
$ python3 scripts/verify-libbun-w1112-review-bundle-20260724.py
FAIL: deterministic evidence replay failed:
OK docs/reviews/libbun-w1112-20260724/vendored-bun-boundary-report.md
OK docs/reviews/libbun-w1112-20260724/adjacent-swarm-source-index.md
OK docs/reviews/libbun-w1112-20260724/lifecycle-vendored-jsc-source-bundle.md
OK docs/reviews/libbun-w1112-20260724/lock-privacy-compliance-index.md
DRIFT docs/reviews/libbun-w1112-20260724/exact-source-search-report.md
DRIFT docs/reviews/libbun-w1112-20260724/process-drop-caller-and-fixture-report.md
exit 1
```

This is a fail-closed verifier defect, not an implementation concern. A bundle
whose required verifier cannot pass in an independent claimed Lane cannot
authorize an expensive review launch.

### 2. The ordered lifecycle inputs contradict the prompt's full-source claim

`lifecycle-prompt.md` says the missing topology is now bound by full Rust
`VirtualMachine`, `VM`, `JSGlobalObject`, and virtual-machine export sources.
The 38-file ordered plan directly attaches only:

```text
vendor/bun/src/jsc/VM.rs
vendor/bun/src/jsc/virtual_machine_exports.rs
```

It does not attach either full source below:

```text
vendor/bun/src/jsc/VirtualMachine.rs  296637 bytes
vendor/bun/src/jsc/JSGlobalObject.rs   69350 bytes
```

`lifecycle-vendored-jsc-source-bundle.md` records their full-file identities,
but supplies only six `VirtualMachine.rs` spans and two `JSGlobalObject.rs`
spans. The verifier checks that the two filenames and selected operation names
occur in that excerpt bundle; `REQUIRED_ATTACHMENTS["lifecycle"]` does not
require either full file. It can therefore accept the report while the model
input lacks the full Rust sources promised by the prompt and required to
inspect the complete VM/global lifecycle topology.

This omission matters to the lifecycle ruling. The review must trace
termination request/reset, event-loop and microtask drain, VM/global teardown,
future and final `Drop`, and shutdown custody without inventing definitions or
unseen callers. Full-file identity plus selected excerpts does not make the
unattached surrounding source available to the implementation model.

The current Oracle dry run already reports 247207 of 272000 tokens, leaving
24793 tokens. The correction must therefore rebudget the lifecycle plan or
create a separately verified lifecycle source sub-bundle if direct attachment
of both complete files crosses the cap; silently retaining excerpts while
claiming full sources is not valid.

## Exact correction

Keep product and test source unchanged, then:

1. Change `command_text()` to emit a Lane-independent command record. Use a
   canonical repository label plus a separately recorded stable working
   directory, or emit `git grep ...` with `working directory: libbun repository
   root`. Do not serialize a claimed worktree path.
2. Regenerate `exact-source-search-report.md` and
   `process-drop-caller-and-fixture-report.md` from that stable representation.
3. Update the generator hash and the regenerated report hashes and byte counts
   in every affected part manifest, Fable plan, synthesis input, and ordered
   dry-run record. Regenerate Oracle dry runs from the identical ordered files.
4. Make the lifecycle attachments truthful and source-complete: attach full
   `VirtualMachine.rs` and `JSGlobalObject.rs`, or create a separately ordered,
   hashed, independently replayable lifecycle source sub-bundle that supplies
   their complete lifecycle definition/caller closure. Rebudget duplicated
   report/excerpt material to remain below 272000 tokens.
5. Extend `REQUIRED_ATTACHMENTS["lifecycle"]` and the lifecycle source checks to
   require the chosen complete-source input, not filename mentions inside an
   excerpt report. Align the prompt's source claim exactly with that input.
6. From a newly claimed independent Lane at the corrected review SHA, run both
   evidence commands. Both must exit zero with no `DRIFT`, and the verifier must
   prove the ordered plan, manifest, Fable rows, report hashes, dry-run identity,
   source identities, and zero product delta at that same SHA.
7. Obtain a new independent lifecycle part review only after those gates pass.
   Keep Oracle and Fable `NOT LAUNCHED` until literal `PART BUNDLE PASS` is
   committed for the corrected bundle.

## Preserved lifecycle and quiescence law

The correction must preserve the existing ruling rather than weakening the
requested implementation:

- `DriveCustody` and `InvocationReadyProof` alone govern same-worker Ready
  settlement after the exact invocation, persistent output pumps, microtasks,
  diagnostics, and finalization barrier are drained.
- `RetirementProof` alone authorizes restart after exact death and complete
  containment/output drain. The attached C++ body proves
  `JSC__VM__deinit` is empty and cannot stand in for retirement or OS-process
  reaping.
- Every forced cancel, deadline, unwind, ambiguous finalization, reaper
  failure, and shutdown conversion retains custody in a typed terminal or
  transfers it by value into `RetirementQuarantine<Purpose>` and
  `DurableReaper` before returning a bounded fault.
- Cooperative interrupt may clear the exact termination request and reuse the
  epoch only after the complete quiescence proof. It must not semantically
  settle authored results inside libbun.
- Future `Drop`, final terminal `Drop`, claim abandonment, reaper retry/panic,
  and shutdown retain one-shot custody. Drop stays nonblocking and silent: it
  may publish intact preallocated custody but may not wait, join, abort,
  allocate, spawn, mint a terminal, expose quarantine identity, or return a
  backend husk.
- Shutdown consumes the backend from every state; shutdown-origin custody
  cannot return to reusable Ready or Restartable state.

The bound adjacent search independently finds
`ManifestResolvedExternalProviderCallAuthority`,
`DurableExternalProviderInvocationAuthority`,
`SsExternalCapabilityProviderHost`, its invocation and shutdown methods, and
`ProviderHostExecutionSession::drop` at swarm
`95323ff17cb29928e31467f651ef03bae2099c14`. Those attached sources and fixtures
must remain in the corrected source-aware bundle.
