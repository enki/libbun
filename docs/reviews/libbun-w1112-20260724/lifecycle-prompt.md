# Oracle/Fable prompt: lifecycle, quarantine, durable reaper, completion claims, restart, and shutdown

You are reviewing libbun at exact candidate 6066a5b85a0c6d1f6397914b8666b0fd0e5fd7eb, tree cb964de8ab8162449fbe95959bf34d231570aa5c. The candidate is the completed poison cut after the rejected W1-11/W1-12 composition: the raw installer is deleted and external compile-fail tripwires exist, but no positive retained-backend implementation exists. Treat the frozen contracts, poison evidence, exact verdict snapshot, source, and search reports as authoritative. Do not invent missing constructors or assume a later tree. The deliverable is CONCRETE IMPLEMENTATION: return an exact-SHA-applicable unified diff or commit-grade replacement code, the exact first positive source edit after poison, migration/deletion order, typed faults and one-shot custody behavior, hostile tests, compile/search commands, and a source-grounded reason for every genuinely impossible hunk. A REVISE or REJECT answer must still supply the clean replacement and largest valid patch plus the exact missing file/symbol/fact list. Preserve Rust ownership of runtime authority and keep observations from feeding authority.

Review and implement the complete lifecycle custody tranche. BunProviderBackend must own private DriveCustody and InvocationReadyProof for fulfilled, rejected, and same-worker cooperative-cancel reuse; forced cancellation, deadline, unwind, active-worker shutdown, and retired faults require RetirementProof or by-value publication-first adoption of RetirementQuarantine<Purpose> by a process-wide preallocated DurableReaper. Public output may contain only bounded QuarantineObservation. Recovery authority remains one private affine QuarantineCompletionClaim<Purpose> supporting pending poll identity, exactly one recovery, abandonment, shutdown conversion, panic requeue, and RetiredDisposal. Restart is legal only after exact retirement proof. Shutdown consumes the backend and cannot expose restart authority.

Drop must be silent and nonblocking: adopt into preallocated durable custody without wait, join, abort, allocation, spawn, public terminal, observation, or claim. Trace refusal, retry, cancellation, deadline, panic/unwind, completion races, claim Drop, recovered-terminal Drop, backend Drop, reaper panic/retry, and shutdown-origin non-recovery. Specify fallible staging before the one infallible publication/commit. Return exact-SHA-applicable commit-grade code and hostile default-parallel tests for every listed lifecycle path, including adoption-before-public-fault and replacement epoch after retirement. Do not solve custody with a public DTO, callback, parts bridge, raw receipt, process abort, blocking destructor, or public quarantine selector.
