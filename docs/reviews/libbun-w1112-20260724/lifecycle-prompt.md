# Correction-6 preserved-pass prompt: JSC interruption/reset, process exit, child-worker quiescence, retained host, quarantine, reaper, and shutdown

You are reviewing the W1-11/W1-12 retained-runtime lifecycle at exact libbun product source `6066a5b85a0c6d1f6397914b8666b0fd0e5fd7eb`, tree `cb964de8ab8162449fbe95959bf34d231570aa5c`, exact correction-5 candidate/verdict SHA `5e74c14a0125c1670be7e37cc31675ebedcd538d`, tree `72f5a93fc3c207ccac42a1113959eb6e1d33e9b3`, and exact adjacent Swarm source `95323ff17cb29928e31467f651ef03bae2099c14`, tree `43b47bbd49a6053d270b3e15cc141cb1b1bb86da`. The correction-5 lifecycle verdict is literal `PART BUNDLE PASS`. Correction 6 preserves this family unchanged and does not reopen its ruling; only source required by the joined union closure may be added. The deliverable remains CONCRETE IMPLEMENTATION with exact-source-applicable code, first edit, typed custody, migration order, hostile default-parallel tests, and exact commands; every rejection must still return the clean replacement and largest valid patch.

The attached complete Rust VM sources and compact exact-item bundle expose both active `BunProcess.cpp` process-exit callers, including `Bun__handleUncaughtException`, and the concrete path `Bun__Process__exit -> node_process::exit -> VirtualMachine::global_exit or WebWorker::exit -> RuntimeHooks::terminate_all_workers_and_wait -> web_worker::terminate_all_and_wait -> live worker register/unregister -> notifyNeedTermination/spin -> ordered WebWorker::shutdown`. The shutdown order unpublishes the VM, runs exit handlers, tears down JSC, unregisters the live worker, dispatches exit, and frees worker resources. `NodeVMModule::evaluate` and NodeVMScript drain microtasks, clear an exception, reset VM termination state, and translate timeout/SIGINT. The bound C++ process/Worker bridges, interruption/reset callers, and the fact that the `JSC__VM__deinit body is empty` are part of the ruling. A termination reset alone is never quiescence proof.

`BunProviderBackend` owns private `DriveCustody`. `InvocationReadyProof` alone authorizes same-epoch reuse after fulfilled, rejected, or cooperatively cancelled execution is proven drained across exact invocation cargo, microtasks, diagnostics, output pumps/barriers, finalization, and all child-worker and nested-worker activity. Cooperative termination timeout or a still-registered worker is ambiguous and must retire; it cannot mint readiness. `RetirementProof` alone authorizes restart.

Every forced cancellation, deadline, unwind, active-worker shutdown, output/join failure, reaper race, future Drop, final terminal Drop, and ambiguous finalization retains custody or transfers intact `RetirementQuarantine<Purpose>` by value to the preallocated `DurableReaper` before returning a bounded typed fault. Observation uses `QuarantineObservation`; recovery is one private affine `QuarantineCompletionClaim`; disposal is `RetiredDisposal`. Shutdown consumes `BunProviderBackend` from every state, and shutdown-origin custody never returns to Ready or Restartable.

The full retained-runtime Rust integration fixture and retained-host checkout/replacement/final-shutdown sources are authoritative. Preserve `ExternalCapabilityProviderPool`, working-directory replacement, exact package-root construction, `.ss` W1-10 hostile inputs, and real linked-worker execution. Supply hostile tests for same-worker reuse, child/nested-worker cooperative termination, timeout and SIGINT reset ambiguity, both process-exit selections, exception-handler unwind, cancellation/deadline/unwind, output barriers, reaper races, replacement epochs, working-directory replacement, final shutdown, and Drop from every future/final terminal. Prove every reusable path is proven drained.
