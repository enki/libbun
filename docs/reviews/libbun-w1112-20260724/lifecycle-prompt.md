# Correction-2 prompt: lifecycle, JSC interruption/reset, quarantine, durable reaper, restart, and shutdown

You are reviewing the W1-11/W1-12 libbun retained-provider full SCC. Exact libbun product source is 6066a5b85a0c6d1f6397914b8666b0fd0e5fd7eb, tree cb964de8ab8162449fbe95959bf34d231570aa5c. Correction-2 review evidence is based on bundle commit ec6a7f249120a833aeaa4e0211fe0f41d17e0565. Exact adjacent Swarm source is 95323ff17cb29928e31467f651ef03bae2099c14, tree 43b47bbd49a6053d270b3e15cc141cb1b1bb86da. The poison cut is complete: install_prepared_export is deleted and external compile-fail tripwires exist, while no positive retained-backend implementation exists. Treat the frozen contracts, three independent REVISE verdict records, deterministic search reports, exact source snapshots, and current source as authoritative. The deliverable is CONCRETE IMPLEMENTATION: provide an exact-source-applicable unified diff or commit-grade code across every repository that must move, the exact first positive edit after poison, typed-fault and one-shot custody behavior, migration/deletion order, hostile default-parallel tests, and exact compile/search/package commands. REVISE or REJECT must still provide the clean replacement and largest valid patch plus the exact missing file/symbol/fact list.

The prior lifecycle review is the exact empty-tree BUNDLE REVISE commit 7a5bfc1cf71299681a9edfb8d4f5a8a7501494e1; its commit object and correction ruling are attached. The missing topology is now bound: full Rust VirtualMachine, VM, JSGlobalObject, and virtual-machine export sources; exact C++/Zig lifecycle excerpts with blob/full-file/excerpt hashes; all libbun process/thread/Drop callers; and the external producer/consumer/process/shutdown/fixture graph.

Preserve private DriveCustody and InvocationReadyProof for fulfilled, rejected, and cooperative-cancel same-worker reuse. Cooperative interrupt may use JSGlobalObject::request_termination or VM::notify_need_termination and may clear the termination request only after the exact invocation, microtasks, persistent stdout/stderr/Bun-output/log pumps, diagnostics, finalization, and per-invocation barriers are proven drained. The attached C++ JSC__VM__deinit body is empty: it is not VM destruction, process retirement, containment drain, output join, or restart proof. Forced cancellation, deadline, unwind, active-worker shutdown, ambiguous finalization, and any unproven drain require exact RetirementProof or by-value RetirementQuarantine<Purpose> adoption by the process-wide preallocated DurableReaper before a bounded public fault.

Public quarantine output is bounded QuarantineObservation only. Recovery remains one private affine QuarantineCompletionClaim<Purpose> with stable pending poll identity, exactly one recovery, abandonment, shutdown conversion, panic requeue, and RetiredDisposal. Restart is legal only after exact retirement proof; shutdown consumes BunProviderBackend and cannot expose restart authority. Drop is silent and nonblocking and may only publish intact preallocated custody: no wait, join, abort, allocation, spawn, terminal, observation, claim, or backend husk.

Return commit-grade lifecycle code and hostile default-parallel tests for cooperative cancellation with proven drain/reuse, forced cancellation replacement epoch, deadline, unwind, output saturation and barriers, adoption-before-fault, single-claim races, pending identity, abandonment, recovered-terminal Drop, shutdown-only claim Drop, reaper spawn/wake/panic/retry, consuming shutdown from every state, and no recovery from shutdown-origin custody. Trace every fallible staging action before one infallible durable publication.
