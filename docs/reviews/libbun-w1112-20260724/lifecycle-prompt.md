# Correction-3 prompt: lifecycle, JSC interruption/reset, retained-host pool, quarantine, durable reaper, restart, and shutdown

You are reviewing the W1-11/W1-12 libbun retained-provider full SCC. Exact libbun product source is 6066a5b85a0c6d1f6397914b8666b0fd0e5fd7eb, tree cb964de8ab8162449fbe95959bf34d231570aa5c. Correction-3 review evidence descends from correction-2 commit 51b0118428d7881f39f32df396ef32349a5a52ab. Exact adjacent Swarm source is 95323ff17cb29928e31467f651ef03bae2099c14, tree 43b47bbd49a6053d270b3e15cc141cb1b1bb86da. The poison cut is complete and no positive retained-backend implementation exists. Treat the contracts, correction-2 lifecycle REVISE at 0a3844d11bb42d67550da0bb1e069ecf17fbe69d, Lane-independent reports, and exact sources as authoritative. Deliver CONCRETE IMPLEMENTATION as an exact-source-applicable unified diff or commit-grade code with the first positive edit, typed-fault/custody behavior, migration/deletion order, hostile default-parallel tests, and exact commands.

The lifecycle plan directly attaches complete VirtualMachine.rs, JSGlobalObject.rs, VM.rs, and virtual_machine_exports.rs, plus exact complete-item C++/Zig interrupt/reset/drain/deinit evidence. It also attaches all libbun process/thread/Drop callers and the complete external retained-host chain: ExternalCapabilityProviderPool construction, working-directory replacement, mutable checkout, provider settlement, runtime execution, final shutdown, and owner Drop. W1-10 ProviderValue remains the sealed by-value input carried by the exact invocation; lifecycle code may not decode or reconstruct it from JSON, Value, strings, bytes, StructuralValue, or parts.

Preserve private DriveCustody and InvocationReadyProof for fulfilled, rejected, and cooperative-cancel same-worker reuse. Cooperative interrupt may use JSGlobalObject::request_termination or VM::notify_need_termination and may clear the exact termination request only after the exact invocation, microtasks, persistent stdout/stderr/Bun-output/log pumps, diagnostics, finalization, per-invocation barriers, and retained-host checkout custody are proven drained. The attached C++ JSC__VM__deinit body is empty: it is not VM destruction, process retirement, containment drain, output join, or restart proof. Forced cancellation, deadline, unwind, active-worker shutdown, ambiguous finalization, and any unproven drain require exact RetirementProof or by-value RetirementQuarantine<Purpose> adoption by the process-wide preallocated DurableReaper before a bounded public fault.

Public quarantine output is bounded QuarantineObservation only. Recovery remains one private affine QuarantineCompletionClaim<Purpose> with stable pending poll identity, exactly one recovery, abandonment, shutdown conversion, panic requeue, and RetiredDisposal. Restart is legal only after exact retirement proof; shutdown consumes BunProviderBackend from every state and cannot expose restart authority. Drop is silent and nonblocking and may only publish intact preallocated custody: no wait, join, abort, allocation, spawn, terminal, observation, claim, or backend husk.

Return commit-grade lifecycle code and hostile default-parallel tests for cooperative cancellation with proven drain/reuse, forced cancellation replacement epoch, deadline, unwind, output saturation/barriers, adoption-before-fault, single-claim races, pending identity, abandonment, recovered-terminal Drop, shutdown-only claim Drop, reaper spawn/wake/panic/retry, retained-host working-directory replacement, final pool shutdown, consuming shutdown from every state, and no recovery from shutdown-origin custody. Trace every fallible staging action before one infallible durable publication.
